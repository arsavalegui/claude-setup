---
paths:
  - "**/agent-flow-app/**"
  - "**/.claude/agent-flow/**"
  - "**/centro-mando/**"
---
Delegar a operador.

Self-check antes de responder:
1. `dist/app.js` y `dist/webview/index.js` del paquete `agent-flow-app` (0.9.1) están vendorizados con parches; los pristines viven en `~/.claude/agent-flow/app.js.pristine` y `webview-index.js.pristine`. Toda edición se regenera con `diff -u` (etiquetas `a/dist/...` `b/dist/...`) contra `~/.claude/agent-flow/agent-flow.patch` (29 hunks), se reaplica con `patch -p1` desde la raíz del paquete, y se reinicia con `systemctl --user restart agent-flow` (Linux) o `launchctl kickstart -k gui/$(id -u)/com.alan.agent-flow` (macOS). En macOS FSEvents junta varias escrituras en un solo aviso: cualquier lógica que dependa del orden entre lecturas del transcript debe funcionar con todo el lote en una sola lectura (por eso `agent_complete` se emite al final de `readSubagentNewLines`).
2. `hook.js` reenvía cada evento a todo `*.json` en esa misma carpeta con el mismo workspace, reescribe `agent_type` a `"<tipo> · <name>"` solo en la copia que va al proceso agent-flow, y deja bitácora en `~/.claude/agent-flow/hooks.log` (una línea JSON por hook, sin contenido): úsala como evidencia de qué hooks llegaron y cuándo.
3. Un agente lanzado con `name` es un teammate en background: "Spawned successfully" no significa que terminó; su fin real es `SubagentStop` o su último assistant con bloque `text` sin `tool_use` pendiente. Un teammate no puede lanzar otro teammate.
4. Verificar SIEMPRE con el rig antes de dar por bueno un cambio: `node ~/.claude/agent-flow/rig/run.js --dist-md5 <md5 de dist/app.js>` (473 casos, instancias aisladas con HOME temporal); para un vistazo rápido, Playwright headless leyendo `window.__afAgents()` y `window.__afSession()` (sondas expuestas por el patch) o `~/.local/bin/pw-shot http://127.0.0.1:3001 out.png --wait 8000 --text 200`. Nunca escribir archivos sintéticos en `~/.claude/projects/` reales.
5. Los nodos de subagentes terminados se desvanecen a propósito (poda upstream): "terminó y ya no está" es correcto; falla solo si aparece vivo tras terminar o si desaparece sin terminar. Límite conocido: un agente muerto a media tool (sin `SubagentStop` ni `SessionEnd`) se muestra vivo hasta 30 min. El nodo PRINCIPAL es distinto: nunca se poda por opacidad (`!a.isMain` en el reductor `cn` de `webview/index.js`) y, desde 2026-09-09, tampoco se declara "ended" solo por 30 min sin actividad en el transcript (`resetInactivityTimer`/`INACTIVITY_TIMEOUT_MS`, `dist/app.js`) — reprograma en silencio mientras el proceso `claude` dueño de la sesión siga vivo. hook.js manda `claude_pid` (= `process.ppid`; Claude Code lanza los hooks como hijos directos, sin shell de por medio, verificado empíricamente) en cada evento hacia el destino agent-flow; el servidor lo guarda en `claudePidBySession` y lo consulta con `process.kill(pid,0)` en cada vuelta del timer (`isClaudeProcessAlive`). La sesión se declara "ended" en un solo sitio, ese timer: cuando en una vuelta el pid ya no existe o nunca se mandó (comportamiento previo, ej. instancias viejas de hook.js). El hook `SessionEnd` real no la cierra por sí mismo, solo limpia `sessionState`/`hookSubagentsDirCache`; tras cerrar Claude, el nodo principal tarda hasta una vuelta del timer (30 min) en irse. Pendientes conocidos, no bloqueantes: `process.kill(pid,0)` no distingue ESRCH de EPERM, y un pid reusado dentro de la misma vuelta mantendría viva una sesión que ya murió. Rig: env `AGENT_FLOW_INACTIVITY_MS` (leída una sola vez al boot) acorta el timeout SOLO para los 3 casos nuevos del grupo `inactividad` (`is00x`, corren en segundos vía `correrInactividadSesion` en `rig/run.js`); nunca se setea fuera del rig.
6. Nunca abrir el navegador del usuario; dar la URL (127.0.0.1:3001 agent-flow, 127.0.0.1:3002 Centro de Mando). Excepción ya autorizada: el perfil aparte `chromium-agentflow` (`--app=http://127.0.0.1:3001` con flags anti-throttling).
7. Cada cambio en `agent-flow/` (patch, hook.js, rig) se commitea y pushea en el repo `~/.claude` (claude-setup) para que llegue a las demás máquinas.
