---
paths:
  - "**/agent-flow-app/**"
  - "**/.claude/agent-flow/**"
  - "**/centro-mando/**"
---
Delegar a operador.

Self-check antes de responder:
1. `dist/app.js` y `dist/webview/index.js` del paquete `agent-flow-app` (0.9.1) están vendorizados con parches; los pristines viven en `~/.claude/agent-flow/app.js.pristine` y `webview-index.js.pristine`. Toda edición se regenera con `diff -u` (etiquetas `a/dist/...` `b/dist/...`) contra `~/.claude/agent-flow/agent-flow.patch` (26 hunks), se reaplica con `patch -p1` desde la raíz del paquete, y se reinicia con `systemctl --user restart agent-flow` (Linux) o `launchctl kickstart -k gui/$(id -u)/com.alan.agent-flow` (macOS). En macOS FSEvents junta varias escrituras en un solo aviso: cualquier lógica que dependa del orden entre lecturas del transcript debe funcionar con todo el lote en una sola lectura (por eso `agent_complete` se emite al final de `readSubagentNewLines`).
2. `hook.js` reenvía cada evento a todo `*.json` en esa misma carpeta con el mismo workspace, reescribe `agent_type` a `"<tipo> · <name>"` solo en la copia que va al proceso agent-flow, y deja bitácora en `~/.claude/agent-flow/hooks.log` (una línea JSON por hook, sin contenido): úsala como evidencia de qué hooks llegaron y cuándo.
3. Un agente lanzado con `name` es un teammate en background: "Spawned successfully" no significa que terminó; su fin real es `SubagentStop` o su último assistant con bloque `text` sin `tool_use` pendiente. Un teammate no puede lanzar otro teammate.
4. Verificar SIEMPRE con el rig antes de dar por bueno un cambio: `node ~/.claude/agent-flow/rig/run.js --dist-md5 <md5 de dist/app.js>` (470 casos, instancias aisladas con HOME temporal); para un vistazo rápido, Playwright headless leyendo `window.__afAgents()` y `window.__afSession()` (sondas expuestas por el patch) o `~/.local/bin/pw-shot http://127.0.0.1:3001 out.png --wait 8000 --text 200`. Nunca escribir archivos sintéticos en `~/.claude/projects/` reales.
5. Los nodos terminados YA NO se podan en vivo: al completar quedan en `state:'complete'` con un piso de opacidad ~0.35 (antes bajaba a 0 y `dn` los borraba del mapa), visibles atenuados el resto de la sesión mientras la pestaña siga abierta. El piso vive en el reductor de animación `cn` de `webview/index.js` (guardas `s>0`/`Math.max(0,...)` → `s>.35`/`Math.max(.35,...)`; `dn` no cambió, simplemente ya no ve `opacity<=0` para agentes). No se tocó cómo el servidor emite `agent_complete`. Al recargar la página o reconectar el SSE, un agente ya completado en el historial reconstruido sigue sin verse (arranca en `opacity:0` de `Kt`, sin fade-in retroactivo) — eso no cambió y el rig lo sigue tolerando como ausencia válida en las medidas de `recarga`/`reinicio`. Falla solo si un nodo aparece vivo tras terminar o si desaparece sin terminar durante la vista en vivo. Límite conocido: un agente muerto a media tool (sin `SubagentStop` ni `SessionEnd`) se muestra vivo hasta 30 min.
6. Nunca abrir el navegador del usuario; dar la URL (127.0.0.1:3001 agent-flow, 127.0.0.1:3002 Centro de Mando). Excepción ya autorizada: el perfil aparte `chromium-agentflow` (`--app=http://127.0.0.1:3001` con flags anti-throttling).
7. Cada cambio en `agent-flow/` (patch, hook.js, rig) se commitea y pushea en el repo `~/.claude` (claude-setup) para que llegue a las demás máquinas.
